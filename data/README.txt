Put your real Humanitix CSV exports in this folder.
This folder is git-ignored, so your attendee data (names, emails, phone
numbers) is never committed. If this folder is empty, EventOps automatically
uses ../sample_data instead.
